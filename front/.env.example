# Base URL of the Sansyar Go API. The Vite dev server also proxies /api -> :8080.
VITE_API_BASE_URL=http://localhost:8080/api/v1

# MVP renders from local mock data by default. Set to "false" to call the real API.
VITE_USE_MOCK=true
